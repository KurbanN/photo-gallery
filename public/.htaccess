# Allmemories — SPA (React Router) на Apache (PS.KZ и аналоги)
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Существующие файлы и папки (assets, app-config.json, …) — отдаём как есть
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Все остальные пути — index.html (гости /e/slug, кабинет /dashboard, …)
  RewriteRule ^ index.html [L]
</IfModule>

# Кэш собранных ассетов Vite (имена с хешем)
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
</IfModule>

<IfModule mod_headers.c>
  <FilesMatch "\.(html)$">
    Header set Cache-Control "no-cache"
  </FilesMatch>
</IfModule>
